import { tangerine, poppins, tektur } from "@/Config/fonts";
import React from "react";

const Navbar = () => {
  return (
    <main className={` ${tangerine.className} px-4 py-2 bg-gray-500`}>
      <div>
        <section>
          <h1 className="text-4xl font-bold ">InstaCity</h1>
        </section>
        <section>
            
        </section>
      </div>
    </main>
  );
};

export default Navbar;
